/** @type {import('next').NextConfig} */
const nextConfig = {
  // No floating dev badge over the product in dev-mode screenshots and recordings.
  devIndicators: false,
};

export default nextConfig;
